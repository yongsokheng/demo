<% if @entry.errors.any? %>
	$('.errors').html("<%= escape_javascript( render 'shared/error_messages', object: @entry) %>")
<% else %>
	$('.content-<%= @entry.id %>').html("<%= escape_javascript(render 'entries/entry_ajax_edit') %>")
<% end %>